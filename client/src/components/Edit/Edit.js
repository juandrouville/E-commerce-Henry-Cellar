import React from 'react'
//import axios from 'axios'

export default function Edit({id}) {

    // useEffect con axios.get en la ruta del ProductDetail asi traigo todo los detalles ya existentes

    // onSubmit este form hace un PUT a la ruta editProduct con los valores que cambio

    // FORM hacer con react-hook-form

    return (
        <div>
            form to edit existent product
        </div>
    )
}
